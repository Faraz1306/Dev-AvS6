<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- Paramètres de connexion à PostgreSQL -->
        <property name="connection.driver_class">org.postgresql.Driver</property>
        <property name="connection.url">jdbc:postgresql://localhost:5433/MasterAnnonce</property>
        <property name="connection.username">postgres</property>
        <property name="connection.password">password13</property>

        <!-- Dialecte PostgreSQL -->
        <property name="dialect">org.hibernate.dialect.PostgreSQLDialect</property>

        <!-- Affichage des SQL exécutées -->
        <property name="show_sql">true</property>
        <property name="format_sql">true</property>

        <!-- Gestion automatique du schéma (update, create, validate, etc.) -->
        <property name="hbm2ddl.auto">update</property>

        <!-- Déclaration de la classe mappée -->
        <mapping class="com.example.model.Annonce"/>
    </session-factory>
</hibernate-configuration>
